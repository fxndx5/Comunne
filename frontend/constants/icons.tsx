import { Feather } from "@expo/vector-icons";
import { colors } from "./colors";

export const icons = {
  mail: <Feather name="mail" size={20} color={colors.text} />,
  user: <Feather name="user" size={20} color={colors.text} />,
  lock: <Feather name="lock" size={20} color={colors.text} />,
  eye: <Feather name="eye" size={20} color={colors.text} />,
  eyeOff: <Feather name="eye-off" size={20} color={colors.text} />,
};
